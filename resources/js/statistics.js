const statistics = function () {
    const statisticsModule = document.querySelector('.statistics-module');
    if (!statisticsModule) return;
    
    console.log('Statistics module is loaded');
}

export default statistics;